<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Dictée vocale du composer. Demandée à la première utilisation, pas au
         lancement : l'application reste utilisable sans micro. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Sans les trois attributs de sauvegarde ci-dessous, Android copie par
         défaut tout le stockage privé de l'application vers le Google Drive de
         l'utilisateur, historique des conversations et pièces jointes compris,
         et l'emporte vers le téléphone suivant lors d'un transfert. La
         politique de confidentialité affirme le contraire : c'est la
         sauvegarde qui est refusée, pas le texte qui est réécrit. -->
    <application
        android:label="FoxLLM"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <!-- Sans cette déclaration, Android 11 et suivants cachent le
             navigateur à l'application : les liens des réponses ne
             s'ouvriraient nulle part. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Sans cette déclaration, Android 11 et suivants cachent le service
             de reconnaissance vocale à l'application. -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
